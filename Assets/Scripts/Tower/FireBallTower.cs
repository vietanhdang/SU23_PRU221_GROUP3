public class FireBallTower : Tower03
{
    // Start is called before the first frame update
    public override void Start()
    {
        Init(1.7f, 4f);
        base.Start();
    }

    // Update is called once per frame
    public override void Update()
    {
        base.Update();
    }
    public override void Attack()
    {
        base.Attack();
    }
}
